import { ReSizerDirective } from './re-sizer.directive';

describe('ReSizerDirective', () => {
  it('should create an instance', () => {
    const directive = new ReSizerDirective();
    expect(directive).toBeTruthy();
  });
});
